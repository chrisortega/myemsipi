services:
  web:
    build:
      context: .
      dockerfile: Dockerfile # Specify the path to your Dockerfile   
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    environment:
      - GYM_DB_SERVER=host.docker.internal
